class Solution {
    public int countSegments(String s) {
        int max=0;
        if(s.length()==0||s.trim().length()==0){
            return 0;
        }
        String[]str=s.trim().split("\\s+");
        int n=str.length;
        return n;
    }
}